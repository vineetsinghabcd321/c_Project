//write a program to find out area of circle  and circumference if radius is 12cm.

#include <stdio.h>

int main() {
    int radius = 12;
    float area = 3.14 * 12 * 12;
    float circumference = 2 * 3.14 * 12;
     
     printf("the area of circle is %f \n", area);
     printf(" the circumference of circle is %f", circumference);
     return 0;
}