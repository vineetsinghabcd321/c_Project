//find the area of rectangle whose length is 12 and widht is 15cm.

#include <stdio.h>
int main() {
    int length = 12;
    int width = 15;
    int area;
    area = length * width;
    printf("area of reactangle is = %d", area);
    return 0;

}